summary: Test OTLP gRPC profile ingestion with TLS
kill-timeout: 60m

execute: |
  cd "$PROJECT_PATH"
  uv run --frozen --isolated --all-extras \
    pytest --exitfirst --model="test-${SPREAD_TASK##*/}" tests/integration/test_profiling_tls.py -v
